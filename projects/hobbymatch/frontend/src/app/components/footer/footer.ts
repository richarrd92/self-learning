import { Component } from '@angular/core';

/**
 * Footer component (standalone) that displays the application footer.
 *
 * Currently a static component without dynamic behavior.
 */
@Component({
  selector: 'app-footer',
  imports: [],
  templateUrl: './footer.html',
  styleUrl: './footer.css',
})
export class Footer {

}
