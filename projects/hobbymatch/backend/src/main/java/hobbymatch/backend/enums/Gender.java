package hobbymatch.backend.enums;

/**
 * Enum representing a user's gender.
 * Used for user profiles and optional filtering or personalization.
 */
public enum Gender {
    MALE,
    FEMALE,
    OTHER,
    PREFER_NOT_TO_SAY
}
