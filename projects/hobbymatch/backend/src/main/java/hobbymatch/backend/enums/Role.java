package hobbymatch.backend.enums;

/**
 * Enum representing the role of a user in the application.
 * Roles determine access level and permissions within the system.
 */
public enum Role {
    USER,
    ADMIN
}
